# Server
HOST=localhost
PORT=4003
NODE_ENV=development
ALLOWED_ORIGINS=http://localhost:4000,http://localhost:4001,http://localhost:4003,http://localhost:4004

# Add app-specific vars below (DB, API keys, etc.)
